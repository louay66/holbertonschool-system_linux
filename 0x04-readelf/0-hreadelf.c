#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

/**
 * main - read ELF file with exeve
 * @argc: number of args
 * @argv: args  passed to program
 * @env:  the environment
 * Return: -1 in fail and 0 in success
 */
int main(void)
{
   return (EXIT_SUCCESS);
}
